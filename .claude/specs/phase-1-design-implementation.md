Status: PHASE READY — implemented in commit c368617 and verified by reviewer agent 2026-08-28: all 10 §8 checklist items PASS, all 5 exit criteria PASS (tsc + expo export re-run independently), functional scope provably unchanged (store.tsx/types.ts diff empty), 15/16 mockups matched (members-customer.png is the known duplicate, open item 5). 5 advisory notes, none blocking — see review notes below §6. Awaiting Akash's on-device check in Expo Go.

# Phase 1 — Visual Redesign Implementation

## 1. Goal
Implement the Claude Design hand-back (tokens, components, screens) across the app without changing functional scope.

## 2. Scope
- IN: src/theme.ts tokens, src/ui.tsx components, all 5 screens, tab bar, sheets, icons (@expo/vector-icons), fonts (expo-font + Google Fonts), proper bill-summary sheet replacing the Alert
- OUT: any feature change, state-shape change, backend work

## 3. Prerequisites
- Claude Design deliverables received per design-handoff.md §6 (tokens table, component specs, 390×844 mockups)
- Paste/commit the hand-back into this folder as `phase-1-design-tokens.md` before implementation starts

## 4. Steps

All section references (`§`) below are to `.claude/specs/phase-1-design-tokens.md` unless stated otherwise. Every measurement, hex, font name and icon name in this section is copied from that file — none are invented here. Where a screen state has a mockup but no individually numbered component spec (e.g. the booking-confirmed sheet, the edit-rate/add-table sheets), build it from the shared primitives at the tokens/spacing defined and check the result pixel-by-pixel against the matching PNG in `design/mockups/` (list in tokens spec §7) before marking the step done.

### Hard constraints (binding for every step below)
- Visual changes only. Do not touch business logic in `src/store.tsx`, the state shape in `src/types.ts`, or any rule documented in `.claude/memory/app_behaviour.md` (billing formula, loyalty points, booking clash rule, bracket/bye logic).
- The one sanctioned behavioral tweak is the Tables screen re-render tick moving from 15s to 1s (§5.7) — that interval lives in `Tables.tsx`, not `store.tsx`, and does not touch how `minutes`/`amount` are computed at stop time.
- Every §8 checklist item in the tokens spec is binding, not advisory: no `borderRadius` anywhere, no emoji, no `shadowColor`/`elevation`, every rule exactly 2pt (5pt only for the in-play top bar and sheet top border), every button label `textAlign:'left'`, every touch target ≥44pt (84pt slot cells, 46pt bracket rows), brass the only accent fill, timers/money always JetBrains Mono, staff control on every header showing STAFF ON, all five empty states using the dashed box with the exact §5.15 copy.
- All colors/spacing/type must come from `src/theme.ts` tokens (`C`, `S`, `T` — see step 4 below). No hardcoded hex, px, or font-size literal in any screen or in `ui.tsx`.
- Validation-only `Alert.alert` calls that have no corresponding sheet spec in tokens §5 (e.g. Booking's "Pick a time slot", "Please enter your name", "Slot taken"; Tournaments' empty-name guard) are **out of scope** — leave them as native alerts. Only the two flows the tokens spec explicitly redesigns (bill summary §5.16, wrong-PIN handling §5.17) and the one flow whose mockup depicts a sheet (`book-confirmed.png`) change away from `Alert.alert`.

### A. Dependencies & fonts

1. Run `npx expo install expo-font @expo-google-fonts/archivo @expo-google-fonts/jetbrains-mono` from the repo root. `expo install` resolves SDK-54/RN-0.81-compatible versions automatically — do not pin versions by hand. `@expo/vector-icons` (for Ionicons, step D) already ships with `expo`; no separate install needed.
2. In `App.tsx`, import `useFonts` from `expo-font` and the weight objects from both packages:
   ```ts
   import { useFonts } from 'expo-font';
   import { Archivo_400Regular, Archivo_600SemiBold, Archivo_700Bold, Archivo_800ExtraBold } from '@expo-google-fonts/archivo';
   import { JetBrainsMono_400Regular, JetBrainsMono_700Bold } from '@expo-google-fonts/jetbrains-mono';
   ```
3. In the `Main` component, call `const [fontsLoaded] = useFonts({ Archivo_400Regular, Archivo_600SemiBold, Archivo_700Bold, Archivo_800ExtraBold, JetBrainsMono_400Regular, JetBrainsMono_700Bold });` and combine it with the existing store `loaded` gate: change `if (!loaded)` to `if (!loaded || !fontsLoaded)`, reusing the same loading `View`/`Text` already there. Do not render any tab/screen before both are true — every text style in step 4 references these exact font-family strings, so an unguarded render before fonts resolve would fall back to the system font.
4. Note the two 400-weight fonts (`Archivo_400Regular`, `JetBrainsMono_400Regular`) are loaded but unused by the current type scale (§2's table only uses 600/700/800) — load them anyway per this task's instruction; do not invent a use for them.

### B. `src/theme.ts` — tokens

5. Replace the entire `C` export with the object from tokens spec §1 verbatim (`ground`, `surface`, `surface2`, `rule`, `ruleFaint`, `ink`, `inkDim`, `inkFaint`, `inkMute`, `brass`, `brassInk`, `live`, `liveInk`, `felt`, `feltText`, `scrim`). Delete every old key (`bg`, `border`, `green`, `greenDim`, `gold`, `red`, `blue`, `text`, `textDim`, `textFaint`) — grep the repo for each old key name after the change and fix every remaining reference (screens + `ui.tsx` + `App.tsx`).
6. Replace the entire `S` export with the object from tokens spec §3 verbatim (`s1`…`s7`, `radius: 0`, `rule: 2`, `ruleAccent: 5`, `tapMin: 44`, `tapPrimary: 48`, `statusBar: 44`, `header: 72`, `tabBar: 84`, `scrollTop: 116`). Delete the old `pad` key; every old `S.pad` usage becomes `S.s4` (16, the closest old-to-new equivalent — screens using it for standard padding) unless the new component spec for that spot calls for a different token (e.g. screen gutter is `S.s5`, not `S.s4`).
7. Add a new exported `T` object — the type-scale helper — mapping every row of tokens spec §2 to a React Native `TextStyle`. Font family strings must be the exact keys loaded in step 3 (e.g. `'Archivo_800ExtraBold'`, `'JetBrainsMono_700Bold'`); do not also set `fontWeight` on any of these styles (RN can synthesize a faux bold against a specific custom font family and shift the metrics — the weight is already baked into the family name). Caps tokens (`label`, `micro`, `nano`) must include `textTransform: 'uppercase'`:
   ```ts
   export const T: Record<string, TextStyle> = {
     display:     { fontFamily: 'Archivo_800ExtraBold',  fontSize: 34, lineHeight: 34, letterSpacing: -0.34 },
     h1:          { fontFamily: 'Archivo_800ExtraBold',  fontSize: 26, lineHeight: 28, letterSpacing: -0.26 },
     h2:          { fontFamily: 'Archivo_800ExtraBold',  fontSize: 22, lineHeight: 24, letterSpacing: -0.22 },
     h3:          { fontFamily: 'Archivo_700Bold',       fontSize: 18, lineHeight: 22, letterSpacing: 0 },
     h4:          { fontFamily: 'Archivo_700Bold',       fontSize: 17, lineHeight: 22, letterSpacing: 0 },
     body:        { fontFamily: 'Archivo_600SemiBold',   fontSize: 15, lineHeight: 20, letterSpacing: 0 },
     bodySm:      { fontFamily: 'Archivo_600SemiBold',   fontSize: 13, lineHeight: 18, letterSpacing: 0 },
     label:       { fontFamily: 'Archivo_800ExtraBold',  fontSize: 13, lineHeight: 16, letterSpacing: 1.8,  textTransform: 'uppercase' },
     micro:       { fontFamily: 'Archivo_800ExtraBold',  fontSize: 11, lineHeight: 14, letterSpacing: 1.3,  textTransform: 'uppercase' },
     nano:        { fontFamily: 'Archivo_800ExtraBold',  fontSize: 10, lineHeight: 13, letterSpacing: 1.2,  textTransform: 'uppercase' },
     monoXl:      { fontFamily: 'JetBrainsMono_700Bold', fontSize: 34, lineHeight: 34, letterSpacing: 0 },
     monoDisplay: { fontFamily: 'JetBrainsMono_700Bold', fontSize: 44, lineHeight: 44, letterSpacing: 0 },
     monoLg:      { fontFamily: 'JetBrainsMono_700Bold', fontSize: 26, lineHeight: 28, letterSpacing: 0 },
     monoMd:      { fontFamily: 'JetBrainsMono_700Bold', fontSize: 16, lineHeight: 20, letterSpacing: 0 },
     monoSm:      { fontFamily: 'JetBrainsMono_700Bold', fontSize: 13, lineHeight: 18, letterSpacing: 0 },
   };
   ```
8. The `monoLg` "30 on stat tiles" override (§2 footnote) is applied locally in the `StatTile` component (step D.14), not in `theme.ts` — spread `T.monoLg` there and override `fontSize: 30`.
9. Zero radius: confirm nothing in the new `S` reintroduces a radius token, and that step C removes every `borderRadius` in `ui.tsx`.

### C. Header safe-area note (flagged, not silently assumed)

10. The app has no `react-native-safe-area-context` and no `SafeAreaView` today (v1's header used a manual `paddingTop: 60` to clear the iOS status bar/notch). The tokens spec models the status bar as its own 44pt token (`S.statusBar`) separate from the 72pt header. Adding a new safe-area dependency is out of scope for this visual-only phase (not in the dependency list in step A, and `design_decisions.md` favors minimal deps). Resolution: keep the header a normal flow `View` (no absolute positioning, no new dependency) and set its `paddingTop` to `S.statusBar + 8` (i.e. 52, where 8 is the header's own top padding from §5.2) instead of the literal `paddingTop: 8` the spec shows in isolation. This reproduces the mockups' visual spacing without a new package. Flag to Akash/reviewer: this is a deliberate substitution, re-check on a notched device (iPhone) once running.
11. The 84pt tab bar (§5.1) already bakes in "the home-indicator area" per its own spec text — set the tab bar container to a fixed `height: S.tabBar` with `paddingTop: 12` and no extra manual bottom padding (replaces v1's `paddingBottom: 28` approximation).

### D. `src/ui.tsx` — shared components

Rework in place; keep the file name and keep `Row` as a plain layout utility (flex row + gap + wrap, no color/type of its own — no change needed beyond removing any leftover hardcoded values). Remove `Card` entirely (no component in tokens spec §5 corresponds to a generic filled card — replaced per-screen by `ListRow`, or by an inline composition built from primitives where the tokens spec calls for something more specific, per step E).

12. **`Screen`** (§5.2) — header becomes a fixed-height `View`: `height: S.statusBar + S.header` (i.e. `paddingTop` per step 10), `paddingHorizontal: S.s5`, `paddingBottom: 14`, `borderBottomWidth: S.rule`, `borderBottomColor: C.rule`. Left block (`flex:1, minWidth:0`): title `<Text style={T.h1}>` in `C.ink`, `marginTop: 3` gap, subtitle `<Text style={T.micro}>` in `C.inkFaint`, `numberOfLines={1}`. Right block (`flexShrink:0, paddingTop:4`, row, `gap: 8`): rename the existing `right` prop to `headerAction?: React.ReactNode` (it now holds only the optional `+ Table`/`+ Member` ghost button — see step 13's Ghost variant), followed unconditionally by `<StaffControl />` (step 16) rendered by `Screen` itself, not passed in by each screen. This is a deliberate design decision to avoid prop-drilling `staffMode`/`setStaffMode` through every screen: `Screen` calls `useStore()` internally only for this purpose. Body: `ScrollView contentContainerStyle={{ paddingTop: S.s5... }}` — keep existing `paddingBottom` generous enough to clear the 84pt tab bar (e.g. `S.tabBar + S.s5`).
13. **`Btn`** (§5.8) — `kind` union becomes `'primary' | 'secondary' | 'danger' | 'ghost'` (drop `'gold'`: brass is the only accent now, so what was the gold "Start bracket" button becomes `kind="primary"`). Add `size?: 'default' | 'sheet'` — `'sheet'` forces height 52 (used only by sheet CTAs), `'default'` uses the per-kind height (primary 48, secondary/danger 44, ghost 36). Remove `alignItems: 'center'` from the button container style (defaults to `stretch`, required so the label can be left-aligned) and set `justifyContent: 'center'` for vertical centering only. Label `<Text>` gets `textAlign: 'left'`, `paddingHorizontal: 14` (16 for primary/sheet), and the matching `T` token per variant (`label` for primary/sheet, `bodySm`-weight equivalent 12/11 sizes for secondary/danger/ghost per the §5.8 table — build these as one-off overrides of `T.label`/`T.micro` sized per the table, since the table's exact per-variant sizes (13/12/11/11) aren't a single named `T` token). Colors per the §5.8 table (`brass`/`brassInk` primary, transparent/2pt‑rule/`inkDim` secondary, transparent/2pt‑`live`/`live` danger, transparent/2pt‑`brass`/`brass` ghost). `disabled` → `opacity: 0.45`, not pressable, applied as its own rule (not merged with the pressed rule). Pressed (not disabled) → `opacity: 0.82` only, no scale, no color change.
14. **`Chip`** (§5.9) — `minHeight: S.tapMin` (44), `borderWidth: S.rule`, `paddingHorizontal: 13` (mid of the 12–14 range), label `T.bodySm`. Selected: `C.brass` fill + border, `C.brassInk` label. Unselected: transparent, `C.rule` border, `C.inkDim` label. Add an optional `style?: ViewStyle` passthrough so the Tourney screen's display-only player tags (step E.20) can override to the tighter `paddingVertical:7 / paddingHorizontal:10` without a second component; player tags pass no `onPress`.
15. **`SegmentedControl`** (new, §5.10) — props `{ options: { label: string; value: T }[]; value: T; onChange: (v: T) => void; compact?: boolean }`. One `View` with `borderWidth: S.rule, borderColor: C.rule`, row of equal-`flex:1` cells each `borderRightWidth: S.rule` except the last, height `S.tapMin` (44) or 40 when `compact` (used only for the Tourney Rounds⇄Bracket switch per §5.10). Selected cell: `C.brass` fill, label `T.micro` in `C.brassInk`. Unselected: transparent, `T.micro` in `C.inkDim`. Used by: Book's day picker, Tourney's Rounds⇄Bracket switch, the Add-member plan toggle, the Add-table type toggle.
16. **`StaffControl`** (new, §5.3) — self-contained: reads `staffMode, setStaffMode, state.staffPin` from `useStore()`, owns its own local PIN-sheet-open state (safe because only one screen is mounted at a time — no nav lib, `App.tsx` conditionally renders one screen per tab). Pill: 36pt tall, `paddingHorizontal: 10`, `nowrap`. Off: `borderWidth: S.rule, borderColor: C.rule`, transparent fill, `lock-closed-outline` (16pt) + `T.micro` "STAFF" in `C.inkDim`; tap opens the PIN sheet. On: `borderWidth: S.rule, borderColor: C.brass`, `shield-checkmark` (16pt) + `T.micro` "STAFF ON" in `C.brass`; tap calls `setStaffMode(false)` immediately, no confirmation. This removes the standalone PIN sheet and `toggleStaff`/`submitPin`/`Alert.alert('Wrong PIN', …)` logic currently in `App.tsx`'s `Main` — that logic moves into `StaffControl`, built on `Sheet` (step 17) as follows:
    - **PIN sheet** (§5.17), titled "STAFF LOGIN". Local `pin: string` state (accumulate digits, max length 4). Four cells in a row (`flex:1`, `height: 56`, `gap: 10`, `borderWidth: S.rule, borderColor: C.rule`): each filled cell shows a `•` in `{...T.monoLg, fontSize:24}` `C.ink`; the next empty cell (index === pin.length) gets `borderColor: C.brass` instead of `C.rule`.
    - Numeric pad below: 3×4 grid (`gap: 8`), each key `56×56`, `borderWidth: S.rule, borderColor: C.rule`, transparent fill, `{...T.monoLg, fontSize:22}` in `C.ink`. Keys in reading order: `1 2 3 / 4 5 6 / 7 8 9 / ⌫ 0 OK`. Digit keys append to `pin` (no-op past length 4). `⌫` removes the last digit. `OK` (and auto-submit when the 4th digit is entered, matching v1's `onSubmitEditing` auto-check convenience) compares `pin` to `state.staffPin`: match → `setStaffMode(true)`, close the sheet, reset `pin`; no match → **reset `pin` to `''` silently** — no `Alert.alert`, no red/error flash on the cells (§5.17: "A wrong PIN clears the field (no error color flash)").
    - `T.micro` hint line beneath the pad, `C.inkFaint`: **"DEMO PIN 1234"** verbatim — do not reference a settings/change-PIN screen anywhere in this sheet (that's phase-2 scope per `design-handoff.md`'s v2 list).
17. **`Sheet`** (§5.13) — `surface` fill, `borderTopWidth: S.ruleAccent (5), borderTopColor: C.brass`, `borderRadius: 0`. Title row: fixed `height: 72`, `paddingHorizontal: S.s5, paddingVertical: 16`, `borderBottomWidth: S.rule`, `T.label` (caps) title flush left in `C.ink`, and a 36pt secondary `chevron-down-outline` (18pt) close button on the right (replace the current `✕` text glyph — an emoji-adjacent glyph the §8 checklist would also flag). Body: `View` with `paddingTop: 18, paddingBottom: 26, paddingHorizontal: S.s5, gap: 16` wrapping the existing `ScrollView`. Backdrop: `C.scrim`, tap-to-dismiss (unchanged behavior). **Keep the existing `KeyboardAvoidingView` wrapper and `behavior={Platform.OS === 'ios' ? 'padding' : undefined}` exactly as-is** — that was added for the keyboard-covers-button fix and is not part of this visual pass.
18. **`Input`** (§5.12) — height 48, `backgroundColor: C.surface`, `borderWidth: S.rule, borderColor: C.rule`, `paddingHorizontal: 14`, text `T.body` (but at `fontSize: 16` per the spec's explicit override — use `{...T.body, fontSize: 16}`) in `C.ink`, placeholder `C.inkFaint`. Focus (`onFocus`/`onBlur` local state): border becomes `C.brass`. `selectionColor: C.brass`. Numeric/phone inputs (`keyboardType="number-pad"` or `"phone-pad"`): apply `{...T.monoMd, fontSize: 16}` instead of the Archivo body style — add an optional `mono?: boolean` prop to `Input` for call sites to opt in (Booking's phone field, Tables' rate fields, the PIN sheet's own cells which are custom, not this `Input`). Keep `Label` as a small helper: restyle to `T.micro` in `C.inkFaint`, `marginBottom: 7`.
19. **Status `Badge`** (new, §5.14) — props `{ kind: 'inPlay' | 'free' | 'live' | 'signup' | 'finished' | 'monthlyPass' | 'regular'; label: string }`, `T.nano`, `padding: 5` vertical / `8` horizontal, `borderRadius: 0`. Style table per kind exactly as §5.14: `inPlay`/`live` → `C.live` fill + `C.liveInk` label (`inPlay` additionally prefixes a 7×7pt solid `C.liveInk` square, `marginRight: 6`); `free` → `borderWidth: S.rule, borderColor: C.felt`, label `C.feltText`; `signup` → `borderWidth: S.rule, borderColor: C.brass`, label `C.brass`; `finished` → `C.rule` fill, label `C.inkDim`; `monthlyPass` → `C.brass` fill, label `C.brassInk`; `regular` → `borderWidth: S.rule, borderColor: C.rule`, label `C.inkDim`.
20. **`Empty`** (§5.15) — change signature to `{ heading: string; body: string }` (no more freeform single `text` prop) so callers must supply both lines. `borderWidth: S.rule, borderStyle: 'dashed', borderColor: C.rule`, `padding: 22` vertical / `18` horizontal, `gap: 6`. Line 1 `T.label` (caps) in `C.inkDim`; line 2 `{...T.bodySm, fontSize: 13}` in `C.inkFaint`. Every call site must use the exact copy from the §5.15 table (reproduced in step E per screen) — no paraphrasing.
21. **`StatTile`** (new, §5.4) — props `{ label: string; value: string; tone?: 'brass' | 'live' | 'ink' }`. Rendered inside a `flexWrap:'wrap'` row by the caller (Home screen), each tile `width: '50%'`, `paddingVertical: 18, paddingHorizontal: 20`, `borderRightWidth: S.rule, borderBottomWidth: S.rule, borderColor: C.rule` applied uniformly to every cell including the outer/last ones (matches §5.4 literally — the outer border reads as the grid's edge, do not special-case corner cells). Label `T.micro` in `C.inkFaint`, `marginTop: 6` gap, value `{...T.monoLg, fontSize: 30}` colored by `tone` (`brass` for money, `live` for a non-zero in-play count, `ink` for everything else including a zero in-play count — the caller decides `tone`, not the component).
22. **`ListRow`** (new, §5.5) — props `{ leading?: string; title: string; meta?: string; trailing?: React.ReactNode }`. `paddingVertical: 13, paddingHorizontal: S.s5`, `borderTopWidth: S.rule, borderTopColor: C.rule`, row layout with `gap: 13`. `leading` (when present): fixed `width: 62`, `T.monoMd` in `C.brass`. Middle `flex:1, minWidth:0`: title `T.body` in `C.ink`, `marginTop: 3`, meta `T.micro` in `C.inkFaint` (when present). `trailing`: caller passes either a `<Text style={[T.monoMd, {color: C.brass}]}>` amount or a 44pt `Btn` — `ListRow` just reserves the slot, it does not style trailing content itself.

### E. Screens — Tables first

Build every table unit, member row, tournament card, bracket, and slot rail **inline** in its screen file using the primitives from step D (`Btn`, `Chip`, `Badge`, `SegmentedControl`, `Input`) plus `C`/`S`/`T` directly — the task's shared-component list for `ui.tsx` is `StatTile` and `ListRow` only; everything else in §5 is a screen-local composition, not a new generic `ui.tsx` export.

**E1. Tables (`src/screens/Tables.tsx`) — do this screen first**

23. Change the tick interval from `setInterval(() => setTick((t) => t + 1), 15000)` to `1000` (§5.7 "tick every second"). This is the one sanctioned behavioral change (hard constraints, above) — `stopSession` in `store.tsx` still computes `minutes`/`amount` from real elapsed `Date.now()` at stop time, unaffected by render-tick frequency.
24. Sort tables so in-play units render before free units: `[...state.tables].sort((a, b) => (b.sessionStart != null ? 1 : 0) - (a.sessionStart != null ? 1 : 0))` (stable sort keeps original order within each group).
25. **Free unit** (§5.6): `paddingVertical: 16, paddingHorizontal: S.s5`, `borderBottomWidth: S.rule, borderBottomColor: C.rule`, `backgroundColor: C.ground`. Row 1: name `T.h3` in `C.ink` over meta `T.micro` "{SNOOKER|POOL} · ₹{rate}/HR" in `C.inkFaint` (left, `flex:1`); right — `<Badge kind="free" label="FREE" />`. Row 2 (staff only, `marginTop: 12`): `<Btn kind="primary" label="Start" style={{flex:1}} onPress={() => openStart(t)} />` then `<Btn kind="secondary" label="Rate" onPress={...} />` (`paddingHorizontal: 16` per §5.8's secondary spacing) — a `Row`-style flex row with `gap: 8` or similar between them.
26. **In-play unit** (§5.7): `backgroundColor: C.surface2`, a 5pt solid `C.live` bar as a separate `View` (`height: S.ruleAccent, backgroundColor: C.live`) at the very top of the unit (i.e. the unit is a column: live-bar View, then a padded content View — do not try to fake it with `borderTopWidth`, use an actual View so the bar spans full width regardless of content padding). Content: `paddingTop: 16, paddingBottom: 18, paddingHorizontal: S.s5`, `gap: 14`. Row 1: name `T.h2` in `C.ink` over meta `T.micro` in `C.inkFaint` (left); right — `<Badge kind="inPlay" label="IN PLAY" />`. Row 2 sits below a `borderTopWidth: S.rule` divider with `paddingTop: 14`: left column — `T.micro` "ELAPSED" in `C.inkFaint`, `T.monoXl` timer (`fmtDuration(elapsed)`) in `C.ink`, then customer name at `{...T.body, fontSize:14}` in `C.inkDim`; right column, **staff only** — `T.micro` "RUNNING BILL" in `C.inkFaint`, `T.monoXl` `fmtMoney(runningBill)` in `C.brass`. Row 3, **staff only**: full-width `<Btn kind="primary" .../>` at `height: 48` (§5.7 explicitly specifies 48 here — this button is inline in the table unit, not in a sheet, so do not use the `size="sheet"` 52pt override) label "Stop & bill" flush left at 16pt padding, calling `doStop(t)` (step 27).
27. **Bill summary sheet** (§5.16) replaces the `Alert.alert` in `doStop`. Add local state `const [billSale, setBillSale] = useState<SaleRecord | null>(null)`. Change `doStop` to `const sale = stopSession(t.id); if (sale) setBillSale(sale);` (delete the `Alert.alert(...)` call). Render `<Sheet visible={billSale != null} onClose={() => setBillSale(null)} title="BILL SUMMARY">` containing four rows (`paddingVertical: 13, borderBottomWidth: S.rule`): `T.micro` caps label in `C.inkFaint` left / `T.monoMd` value in `C.ink` right, for **Table** (`billSale.tableName`), **Customer** (`billSale.customer`), **Minutes** (`billSale.minutes`), **Rate** — `SaleRecord` does not persist a rate field and must not gain one (`types.ts` is frozen scope), so derive the displayed rate as `state.tables.find(x => x.id === billSale.tableId)?.hourlyRate` at render time. Flag: this is a live lookup, not the historical rate — correct in the overwhelming common case (rate isn't edited in the seconds between stop and viewing the sheet) but would show today's current rate if staff changed it mid-flow; v1's alert didn't show a rate at all, so this is a net addition, not a regression. Then the total block (`paddingTop: 20, paddingBottom: 22`): `T.label`-styled "TOTAL" (12pt per §5.16, i.e. `{...T.label, fontSize: 12}`) in `C.ink` left, `T.monoDisplay` amount in `C.brass` right, both baseline-aligned in a row. Close with a full-width `<Btn kind="primary" size="sheet" label="Mark paid" onPress={() => setBillSale(null)} />` (no separate "paid" state to persist — this sheet is purely a confirmation moment, `stopSession` already recorded the sale).
28. **Start-session sheet** (§5.18): retitle "START SESSION". Keep the existing customer `Input` and member `Chip` quick-pick logic (`openStart`/`confirmStart` unchanged), restyle the member picker section under a `T.micro` "MEMBERS" label. Add a summary row above the CTA: `borderTopWidth: S.rule, paddingTop: 13` — left `T.micro` "{startFor?.name} · {SNOOKER|POOL}", right `T.monoMd` "₹{rate}/HR" in `C.brass`. CTA: `<Btn kind="primary" size="sheet" label="Start session" onPress={confirmStart} />`. Empty-customer-defaults-to-"Walk-in" behavior is already in `store.tsx`'s `startSession` — no change needed.
29. **Edit-rate sheet**: restyle onto the `Sheet`/`Input`/`Btn` primitives (title "EDIT RATE", numeric `Input` with `mono` prop, `<Btn kind="primary" size="sheet" label="Save rate" .../>`). No spec section number of its own — match `tables-edit-rate-sheet.png`.
30. **Add-table sheet**: restyle similarly (title "ADD TABLE", name `Input`, type `SegmentedControl` with options Snooker/Pool replacing the current `Chip` pair, rate `Input` with `mono`, `<Btn kind="primary" size="sheet" label="Add table" .../>`). Match `tables-add-table-sheet.png`.
31. Header (`<Screen title="Tables" subtitle={...} headerAction={staffMode ? <Btn kind="ghost" label="+ Table" onPress={...}/> : undefined}>`): subtitle is `` `${activeTables.length} OF ${state.tables.length} IN PLAY` `` per §5.2's table — dynamic, not literal text (the count must reflect real state).

**E2. Home / Dashboard (`src/screens/Dashboard.tsx`)**

32. Replace the local `Stat` component with `StatTile` (step D.21). Render inside a `View style={{flexDirection:'row', flexWrap:'wrap'}}`: customer sees 2 tiles (`In play` tone `live` when `activeTables.length > 0` else `ink`; `Bookings today` tone `ink`); staff additionally sees `Earnings today` (tone `brass`) and `Members` (tone `ink`) — same 4-tile grid, not two separate rows, so the rule-border grid lines read continuously per §5.4.
33. Header title is literal **"Kakul"** (not `state.clubName`, which stays the full legal/display name used elsewhere) — this is the tokens spec's short wordmark-style title (§5.2 table), a deliberate divergence from v1's full-name header, flagged here since it's not obviously derivable from state. Subtitle stays **dynamic** (not the literal placeholder text shown in the spec table): `` `OPEN ${fmtHour(state.openHour)} – ${fmtHour(state.closeHour)} · ${weekday}` `` where `weekday` is computed via `new Date().toLocaleDateString('en-US', { weekday: 'short', day: 'numeric', month: 'short' })` (the `T.micro` style's `textTransform:'uppercase'` handles the caps automatically — don't manually uppercase). Keeping hours dynamic (vs. hardcoding "10 AM – 11 PM" as literal copy) respects the placeholder-data constraint in Open Item 2.
34. "Today's bookings" → list of `ListRow`s: `leading={fmtHour(b.hour)}`, `title={b.customerName}`, `meta={table?.name}`. Empty state: `<Empty heading="NO BOOKINGS YET" body="Slots open from 10 AM. Book one from the Book tab." />` (§5.15 table row 1 — note this copy is fixed regardless of the dynamic open-hour subtitle above; it's a separate literal per the table).
35. "Tournaments" → `ListRow`s: `title={t.name}`, `meta={`${t.dateISO} · ${t.players.length} PLAYERS · ${fmtMoney(t.entryFee)} · ${t.status === 'signup' ? 'SIGN-UPS OPEN' : 'LIVE'}`}`. Empty: `<Empty heading="NO TOURNAMENTS" body="Staff can add one from this screen." />`.
36. "Recent billing" (staff only) → `ListRow`s: `title={s.customer}`, `meta={`${s.tableName} · ${s.minutes} MIN`}`, `trailing={<Text style={[T.monoMd,{color:C.brass}]}>{fmtMoney(s.amount)}</Text>}`. Empty: `<Empty heading="NOTHING BILLED TODAY" body="Sessions you stop will show up here." />`.

**E3. Book (`src/screens/Booking.tsx`)**

37. Day picker → `SegmentedControl` with 3 options. Fix the third label: v1 computes `todayISO(2).slice(5)` (renders as "MM-DD"); the spec wants the weekday name (§5.10: "Today / Tomorrow / {weekday}"). Compute it as `new Date(Date.now() + 2*86400000).toLocaleDateString('en-US', { weekday: 'short' })`. This is a copy/logic fix scoped to the screen component, not a state-shape or business-logic change.
38. Table picker: keep the existing wrapped `Chip` row (via `Row`) — the tokens spec does not call out the table picker as a redesign target (only the slot picker is, §2 of `design-handoff.md`), so only its visual restyle (step D.14) applies here, no layout change.
39. **Slot rail** (§5.11) — replace the wrapping `Row` of 13 `Chip`s with a horizontal `ScrollView` (`horizontal`, `showsHorizontalScrollIndicator={false}`, `contentContainerStyle={{paddingHorizontal: S.s5, gap: 8}}`, `ref` for auto-scroll). Each cell: `84×84`, `borderWidth: S.rule`, `padding: 10`, `justifyContent:'space-between'` column, both lines `textAlign:'left'`. Top line: hour formatted with no space, e.g. `10AM`/`1PM` (new formatter — `fmtHour` currently returns `"10 AM"` with a space; add a slot-only formatter or strip the space inline), `T.monoMd` at `fontSize: 18` (`{...T.monoMd, fontSize:18}`) — free/selected/taken colors per the §5.11 table (`inkDim`/`brassInk`/`inkMute`). Bottom line: `T.nano` state label "FREE"/"SELECTED"/"TAKEN". Fill/border per state: free → transparent / `C.rule`; selected → `C.brass` / `C.brass`; taken → `C.surface` / `C.ruleFaint`, `disabled` (no `onPress`). A right-aligned `T.micro` "SWIPE →" hint sits above the rail (only render it once — not per re-render churn). Auto-scroll: in a `useEffect` keyed on `[dateISO, tableId]`, find the first hour not in `takenHours`; if found and its index `> 0`, call `scrollRef.current?.scrollTo({ x: index * (84 + 8), animated: true })` (cell width 84 + gap 8 = 92pt stride, both values already defined above — no new invented constant).
40. **Booking-confirmed sheet** replaces `Alert.alert('Booked!', ...)`. Add local state `const [confirmed, setConfirmed] = useState<{table: string; dateISO: string; hour: number; name: string} | null>(null)`; on successful `addBooking` (no error string returned), set it instead of alerting, and clear the form (`setHour(null); setName(''); setPhone('')`) as before. Render via `Sheet` (title "BOOKING CONFIRMED"); tokens spec §0 states green/felt is reserved for "the FREE outline and the booking-confirmed banner" — build a `C.felt`-bordered (or filled, per `book-confirmed.png` — check the mockup for fill-vs-border since neither is pin-numbered in §5) banner summarizing table/date/time/customer, closed with a `<Btn kind="primary" size="sheet" label="Done" onPress={() => setConfirmed(null)} />`. This is the one sheet in this spec without an itemized §5.x breakdown — match `book-confirmed.png` directly.
41. Upcoming bookings → `ListRow`s: `title={`${b.customerName} · ${table?.name}`}`, `meta={`${b.dateISO} AT ${fmtHour(b.hour)}${staffMode && b.phone ? ' · ' + b.phone : ''}`}`, `trailing={staffMode ? <Btn kind="danger" label="Cancel" onPress={() => cancelBooking(b.id)} /> : undefined}`. Empty: `<Empty heading="NO UPCOMING BOOKINGS" body="Pick a day, table and slot above." />`.
42. Header subtitle: `` `ONE-HOUR SLOTS · ${fmtHour(state.openHour)} – ${fmtHour(state.closeHour - 1)}` `` — dynamic, same placeholder-data rationale as step 33.

**E4. Members (`src/screens/Members.tsx`)**

43. Loyalty banner above the list: `backgroundColor: C.surface2`, row with `T.monoMd` "10 PTS" in `C.brass` + `T.micro` "PER ₹100 PLAYED" in `C.inkDim` (this 10pt/₹100 figure is confirmed app behavior per `app_behaviour.md`, not a placeholder — hardcode the copy).
44. **Member row** (§5.19), built inline: `paddingVertical: 16, paddingHorizontal: S.s5`, `borderBottomWidth: S.rule`, plus a `3pt` (custom, not `S.rule` — use a literal `3` since the spec explicitly calls this out as a one-off "3 pt `brass` vertical bar", distinct from the 2pt rule system) `C.brass` bar as an absolutely-positioned or flex-row leading `View` spanning full row height (`position:'absolute', left:0, top:0, bottom:0, width:3, backgroundColor: C.brass}` on the row, with content padded `paddingLeft` enough to clear it, is the simplest approach). Line 1: name `T.h4` in `C.ink` left; `<Badge kind={m.plan === 'Monthly Pass' ? 'monthlyPass' : 'regular'} label={m.plan === 'Monthly Pass' ? 'MONTHLY PASS' : 'REGULAR'} />` right. Line 2 left: `T.micro` "JOINED {MONTH YEAR} · {n} VISITS" in `C.inkFaint` (derive month/year from `m.joinedISO`), and staff-only phone in `{...T.monoSm, color: C.inkDim}` beneath it. Line 2 right: points `{...T.monoLg, fontSize:26}` in `C.brass`, `T.nano` "POINTS" caption under it, both right-aligned.
45. Empty state: `<Empty heading="NO MEMBERS YET" body="Add the first one with + Member." />` for staff; the spec's §5.15 table only has one Members row — for the customer view (no `+ Member` action visible), keep the existing v1 customer-specific empty copy ("ask at the counter to join") since the tokens spec's fixed copy assumes the staff CTA exists; this is a minor divergence, flag it for review rather than silently forcing staff-only copy onto customers.
46. Header subtitle: `` `${state.members.length} MEMBERS · 10 PTS PER ₹100` `` (member count dynamic, loyalty text literal per step 43's rationale).
47. Add-member sheet: retitle "ADD MEMBER", plan toggle becomes `SegmentedControl` (Regular / Monthly Pass) replacing the `Chip` pair, CTA `<Btn kind="primary" size="sheet" label="Add member" .../>`. Match `members-add-member-sheet.png`.

**E5. Tourney (`src/screens/Tournaments.tsx`)**

48. **Tournament card** (§5.20), built inline, replaces the current `Card`-wrapped block: `borderBottomWidth: S.rule`, no fill. Header block `paddingVertical:16, paddingHorizontal: S.s5`: name `{...T.h3, fontSize:20}` in `C.ink` + status `Badge` right (`kind="signup"` label "SIGN-UPS OPEN" / `kind="live"` label "LIVE" / `kind="finished"` label "FINISHED", mapping from `t.status`); below, a `T.micro` meta row (`gap:16`) — date, "{n} PLAYERS", "{fmtMoney(entryFee)} ENTRY".
49. **Finished state**: full-bleed `C.brass` banner under the header: `T.micro` "CHAMPION" over `{...T.display, fontSize:24}` name, both in `C.brassInk`. Remove the current `🏆 Champion: {name}` text/emoji entirely (checklist item: no emoji anywhere).
50. **Sign-up state**: player tags via `Chip` in display-only mode (`style` override per step D.14, no `onPress`) instead of the current comma-joined text string; then the existing add-player `Input` + `<Btn kind="secondary" label="Add" .../>` (44pt, replacing "Join"); then staff-only `<Btn kind="primary" label="Start bracket" disabled={t.players.length < 2} .../>` (drop the old `kind="gold"` and the player-count suffix in the label — the spec doesn't call for a dynamic count in the CTA label; keep the count visible via the player tags instead).
51. **Running state — Rounds view** (§5.21, default view): vertical list, each round a `T.micro` caps header in `C.inkFaint` above a `borderBottomWidth: S.rule`, then match blocks: `borderWidth: S.rule` box containing two `46pt` rows split by a `borderBottomWidth: S.rule` (only between the two rows, not around them individually). Each row: player name `{...T.body, fontSize:15}` flush left, a `T.nano` mark right. Undecided match, staff: both rows are `Pressable`s (`onPress={() => recordWinner(t.id, m.id, name)}`), transparent, `C.ink`; customers see the same rows not pressable (just `View`, no `Pressable`). Decided: winner row `C.brass` fill + `C.brassInk` name + mark "WON"; loser row transparent + `C.inkFaint`. Bye (`m.p2 === null`): second row reads "Bye" in `C.inkMute` with mark "AUTO" (replace the current inline `"(bye)"` string); first row already shows as the winner per the decided styling (since `store.tsx`'s `pairRound` already auto-sets `winner` for byes — no logic change, just the new visual treatment). Tap target for each row is the full 46pt row (`hitSlop` not needed since the row itself is already ≥44pt).
52. **Running state — Bracket view** (§5.21, read-only): horizontal `ScrollView` of round columns, `168` wide each, `gap:14`, each column a `T.micro` header over match boxes laid out with `justifyContent:'space-around'`. Rows `34pt`, `{...T.body, fontSize:13}`, same win/loss colors as Rounds. No `onPress` anywhere in this view — recording only happens in Rounds (keeps every recording tap at the full 46pt target, per the tokens spec's explicit rationale).
53. Switch between the two views with the `compact` `SegmentedControl` (step D.15) — local screen state, not persisted.
54. Header subtitle: literal `"KNOCKOUT · SINGLE ELIMINATION"` (no state dependency, safe to hardcode per §5.2 table).
55. Empty state (no tournaments): `<Empty heading="NO TOURNAMENTS" body="Staff can add one from this screen." />` — same copy as the Home screen's tournaments empty state (step 35); both map to the single "Tournaments" row in the §5.15 table.

### F. `App.tsx` — tab bar & staff entry point

56. Remove the floating `staffBadge` `Pressable`, the `toggleStaff`/`submitPin`/`pin`/`pinOpen` state, and the standalone PIN `Sheet` from `Main` entirely — this logic now lives in `StaffControl` (step D.16), rendered by every `Screen` header.
57. Import `Ionicons` from `@expo/vector-icons/Ionicons` (or `@expo/vector-icons`) and replace the emoji `TABS` array (`⌂ 🎱 📅 👥 🏆`) with the icon names from tokens spec §4: `home-outline`, `grid-outline`, `calendar-outline`, `people-outline`, `trophy-outline` — all 24pt.
58. **Tab bar** (§5.1): container `height: S.tabBar` (see step 11), `backgroundColor: C.ground`, `borderTopWidth: S.rule, borderTopColor: C.rule`, row of 5 `flex:1` cells (the whole cell is the touch target, not just the icon/label). Each cell, `paddingTop: 12`, column, `gap: 5`, centered: a `24×3pt` mark `View` (`backgroundColor: 'transparent'` when inactive so layout never shifts — this reserved space is why the mark is a real element, not conditionally rendered), the 24pt `Ionicons` icon, then `T.micro` label. Active: mark + icon + label → `C.brass`. Inactive: mark stays transparent, icon + label → `C.inkFaint`. Pressed (not active): icon + label at `C.brass` with `opacity: 0.6` (this is the one place opacity applies to color, not the whole cell, per §5.1's literal wording "label and icon brass at 60% opacity").
59. `StatusBar style="light"` stays as-is (unrelated to the visual token system).

### G. Verification & handoff

60. Search the full diff for `borderRadius`, emoji ranges/`✕`/`🏆`/`●`/`⌂`/`🎱`/`📅`/`👥`, and `shadowColor`/`elevation` — every tokens-spec §8 checklist item must return zero hits (aside from `radius: 0` itself in `theme.ts`, which is fine).
61. Run `npx tsc --noEmit` — must be clean.
62. Run `npx expo export --platform ios` — must succeed.
63. Manually re-walk `design-handoff.md` §4 (every screen/flow) against the running app (or the export) to confirm functional scope is identical to v1 — same booking clash behavior, same billing math, same loyalty crediting, same bracket/bye logic, same PIN gate (`1234` still works), nothing added or removed beyond the visual system.
64. Update `.claude/memory/design_system.md` — mark the "Things that changed structurally" list as implemented (it currently describes the target state prospectively; confirm it now matches the shipped code) and note the tick-interval change (15s → 1s) since that's a behavior-adjacent detail worth recording even though it's UI-only.
65. Update `.claude/memory/project_context.md` — "Current Stage" line: move from "Visual redesign in progress" to reflect Phase 1 implementation complete (once the implementer finishes) or in progress (if this spec is committed before implementation starts).
66. `.claude/memory/app_behaviour.md`, `.claude/memory/design_decisions.md`, and `.claude/memory/tables_rates.md` should need **no** content changes (no behavior, decision, or data-value changed) — the implementer should explicitly confirm this rather than skip the file, per the "always update memory" rule: confirming "no change needed" counts as the required check.
67. Update `README.md`'s "Project handoff & sync" → "Current state" section to describe the Felt & Brass UI, the new deps (`expo-font`, `@expo-google-fonts/archivo`, `@expo-google-fonts/jetbrains-mono`), and the tick-interval change; update "Pending / next" if the redesign was the item blocking it.

## 5. Exit criteria
- All colors/type/spacing come from theme tokens (no hardcoded values in screens)
- Icon set + fonts match the hand-back exactly (names documented)
- Functional scope identical to design-handoff.md §4 (reviewer walks every screen/flow)
- `npx tsc --noEmit` and `npx expo export --platform ios` pass
- Touch targets >= 44pt (slot picker and bracket taps explicitly checked)

## 6. Rollback
- Single revert of the phase's commits restores v1 visuals; no data/state impact.

## Review notes (reviewer agent, 2026-08-28 — advisory, none blocking)
1. FREE-table "Start" button renders 48pt (default primary Btn) vs tokens §5.6's stated 44pt — clears the ≥44pt minimum; spec-internal inconsistency (§5.6 vs §5.8), not a defect.
2. Day-picker third label renders "FRI" (weekday only) vs mockup's "FRI 30" — implementer followed spec step 37 verbatim; the step itself diverged from the mockup.
3. Booking-confirmed sheet omits the mockup's "Arrive five minutes early; the table is held for ten." sentence — no §5.x section governs this sheet.
4. Tourney "+ New" header ghost button lacks the add-outline icon that "+ Table"/"+ Member" carry — cosmetic consistency only, unspec'd.
5. Two literal `borderRadius: 0` in ui.tsx (enforce zero radius) — compliant, noted for grep hygiene.

## Open items from the design hand-back (none blocking)
1. Zero radius — CONFIRMED by Akash 2026-08-27 (see memory/design_decisions.md).
2. Club data (rates/hours/PIN) — still placeholders; mockups + tokens spec use ₹200/₹150, 10 AM–11 PM, PIN 1234. Real values are phase-2 scope; do not hardcode anything new that makes them harder to change.
3. Add-table and Add-member sheets — RESOLVED 2026-08-27: mockups delivered in hand-back v2 (tables-add-table-sheet.png, members-add-member-sheet.png).
4. Tourney bracket — Akash chose to skip the horizontal Bracket view capture; Rounds view PNG is the reference (staff tap there).
5. `members-customer.png` in the hand-back is a byte-identical duplicate of `book-customer.png` (confirmed via checksum) — Members customer-view mockup is missing. Implement from §5.19/§6 + `members-staff.png` minus staff surfaces (no phone, no + Member, STAFF control off). Request corrected export from Claude Design (non-blocking).
